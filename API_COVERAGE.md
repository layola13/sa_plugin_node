# Node Plugin API Coverage

This plugin is a native SA compatibility layer for common Node.js APIs. It does not execute JavaScript and does not depend on Node, V8, or VM APIs.

## Implemented Native Surfaces

- `os`, `process`, `path`, `querystring`, `url`, `punycode`, `util`, `buffer`, `string_decoder` expose native ABI functions and SA macros. `os.constants` now exposes structured signal, errno, priority, dlopen, and libuv constants; `os.getPriority`/`setPriority` use POSIX process priority APIs; `process.kill` sends real POSIX signals; `process.resourceUsage` exposes `getrusage(2)` JSON; `process.availableMemory`/`constrainedMemory` read Linux memory and cgroup limits; `process.features` exposes the native build capability subset; and `util.callbackify/promisify`, `util.parseArgs`, `util.mimeType`, `buffer.resolveObjectURL`, and `fs.opendir`/`opendir_next` have concrete native behavior.
- `fs` exposes sync-style file, directory, fd, metadata, link, glob, and statfs operations. Ownership, fd sync, timestamp, and filesystem-stat calls use POSIX/libc `chown`, `fchown`, `fsync`, `fdatasync`, `futimens`, `utimensat`, and `statvfs` behavior. `fs.promises` exposes promise-module ABI entry points for the same common file, directory, metadata, fd, and `FileHandle`-style operations, returning already-resolved native results to SA code.
- `crypto` exposes random bytes/UUID/int/fill, hash/HMAC state, PBKDF2, HKDF, scrypt, simple cipher/decipher handles, Ed25519 sign/verify, key generation, hash listing, and a Web Crypto sync subset for `getRandomValues`, `randomUUID`, `subtle.digest`-style hashes, raw key import/generation/export, HMAC-SHA256 sign/verify, Ed25519 sign/verify, and AES-256-GCM encrypt/decrypt.
- `net` exposes TCP connect/listen/accept/read/write/end, Unix domain socket path connect/listen with server-close unlink cleanup, server `address()`/`listening` state, close semantics, connection counts, and `maxConnections` metadata/drop behavior including Node v21+ `maxConnections = 0` drop-all semantics, shared `ref`/`unref`/`hasRef` handle state for sockets and servers, local/remote address metadata and Node-style local/remote address/family/port getters, `bytesRead`/`bytesWritten` counters, `bufferSize`/`connecting`/`pending`/`readyState` state helpers, `autoSelectFamilyAttemptedAddresses`-style attempted remote address JSON for synchronous outbound TCP connects, socket `readable`/`writable`/`closed`/`destroyed` state helpers, non-freeing `destroy`, `setTimeout`/timeout metadata backed by socket receive/send timeouts, TCP `resetAndDestroy` via linger-reset close, `setNoDelay`, `setKeepAlive`, send/receive buffer size configuration, `setTypeOfService`/`getTypeOfService` for connected sockets via `IP_TOS`/`IPV6_TCLASS`, write-side shutdown, `SocketAddress` create/parse/address/family/port/flowlabel/json handles, `BlockList` address/range/subnet/check/rules handles including `SocketAddress` handle inputs, outbound TCP connect `BlockList` checks, server inbound `BlockList` filtering with copied rules, common `createConnection(options)` fields via native `family` selection, `localAddress`/`localPort` bind, `noDelay`, `keepAlive`, timeout, and `blockList`, `isIP` helpers, and default auto-select-family/attempt-timeout module settings. `createConnection` now delegates to TCP connect; `createServer` creates a real listener handle on an ephemeral port. `bufferSize` and `connecting` reflect this ABI's synchronous connect/write model and report zero for established sockets; attempted-address metadata records real addresses tried by this synchronous ABI, not a fake Happy Eyeballs race.
- `dgram` exposes UDP4/UDP6 create/bind/send/recv/close, create-with-options for `reuseAddr`/`reusePort`/`ipv6Only`/send and receive buffer sizing, send/receive `net.BlockList` filtering with copied rules, connect/disconnect, connected send, local/remote address metadata, `ref`/`unref`/`hasRef` handle state, broadcast, TTL, send/receive buffer size configuration, send queue size/count queries, IPv4 multicast TTL/loopback/interface/membership/source-specific membership controls, and UDP6 multicast hops/loopback/interface/membership controls. Send queue size/count report `0` because this plugin's UDP sends complete synchronously instead of queuing libuv send requests.
- `dns` exposes lookup/resolve plus resolver-backed record entry points for A, AAAA, ANY, CAA, NAPTR, SOA, TLSA, CNAME, MX, NS, TXT, SRV, PTR, reverse lookup, IPv6 reverse lookup, lookupService, Node-compatible lookup flags/error-code constants, global resolver server configuration, default result order configuration, `lookup()` option handling for `family`, `hints`, `all`, and `order`, and Resolver instance handles with independent server lists/snapshots/common resolve methods. `ADDRCONFIG`, `V4MAPPED`, and `ALL` lookup hints are implemented within the plugin's synchronous OS resolver model. Resolver instances with custom `setServers` now issue UDP DNS queries to the configured server before parsing records, and `setLocalAddress` validates/stores IPv4/IPv6 bind addresses used by those custom UDP queries. `cancel` is exposed as synchronous Resolver state in this ABI and records calls in snapshots; there is no in-flight async DNS operation to interrupt. `dns.promises` exposes the same native results, constants, global server/default-order configuration helpers, and Promise Resolver handles, returning already-resolved buffers to SA code.
- `http` exposes HTTP/1 client, async client, one-shot `request`/`get` JSON helpers, server, streaming response, and WebSocket bridge by compiling the official `http-client` and `http-server` plugin implementations into `libnode.so`; it also exposes Node-compatible `METHODS`, `STATUS_CODES`, `maxHeaderSize` default metadata, `setMaxIdleHTTPParsers()` parser-pool limit state, and low-level header name/value validators. The one-shot helpers return completed native response JSON and do not emulate Node's asynchronous `ClientRequest` event object.
- `http2` exposes native constants JSON, `sensitiveHeaders` metadata, default settings JSON, `getPackedSettings`/`getUnpackedSettings` helpers for RFC wire-format settings payloads, and a cleartext prior-knowledge client request helper through dynamically loaded `libnghttp2`, returning status, headers, body, and frame count as JSON.
- `quic`/`http3` expose native constants, ALPN/RFC metadata, runtime capability detection for the required `ngtcp2`/`nghttp3`/OpenSSL stack, explicit UDP-backed endpoint handles with bind/connect/listen/address/ref/close/snapshot helpers, and HTTP/3 session metadata handles with authority/path/method snapshots plus datagram send/recv over the underlying native UDP transport. This remains an honest transport/configuration subset rather than a claim of QUIC handshake, congestion control, or HTTP/3 stream framing semantics.
- `dtls` now exposes a native UDP-backed endpoint subset for connect/listen/snapshot/send/recv/close/free using the same explicit handle model as the QUIC compatibility layer, without claiming DTLS handshake, certificate exchange, or record-layer crypto semantics.
- `https` client requests are available through the HTTP client bridge when Zig TLS support is available, including one-shot `request`/`get` JSON helpers for `https://` URLs; `tls` exposes default constants, `DEFAULT_MIN_VERSION`/`DEFAULT_MAX_VERSION`, `convertALPNProtocols()`-style ALPN wire-format encoding, `getCiphers()`-style native cipher listings, native-system `rootCertificates`/`getCACertificates()` JSON, `setDefaultCACertificates()` PEM parsing, `createSecureContext()` handles for CA/cert/key/cipher/version metadata, secure-context snapshots/free, secure-context-backed client verification for custom CA bundles, and a synchronous client socket subset for connect/write/read/close, common `tls.connect(options)` fields via native `family` selection, `localAddress`/`localPort` bind, `noDelay`, `keepAlive`, timeout, and `blockList`, authorization metadata, SNI/servername metadata, ALPN absence metadata, negotiated protocol and cipher metadata, local/remote address/family/port metadata, timeout metadata backed by socket receive/send timeouts, byte counters, `bufferSize`/`connecting`/`pending`/`readyState` inherited socket-state helpers, readable/writable/closed/destroyed state helpers, `ref`/`unref`/`hasRef` handle state, and non-freeing destroy semantics. `bufferSize`, `connecting`, and `pending` reflect this ABI's synchronous TCP connect and TLS handshake model and report zero for established sockets.
- `events`, `stream`, `timers`, `readline`, `console`, `tty`, `perf_hooks`, `diagnostics_channel`, `trace_events`, `async_hooks`, `async_context_tracking`, `worker_threads`, `child_process`, `cluster`, `domain`, `deprecated`, `command_line_options`, `environment_variables`, `errors`, `internationalization`, `iterable_streams`, `test_runner`, `wasi`, `report`, and `sea` expose compatibility handles or status APIs. `events` tracks listener state across base and extra APIs; `stream` and `readline` now use allocated native handles rather than sentinel pointers. `timers.promises` exposes synchronous awaitable-style helpers for setTimeout, setImmediate, scheduler.wait/yield, and setInterval tick handles. `readline.promises` exposes a native Interface handle with buffered input, question, close, snapshot, and free operations. `tty` uses POSIX `isatty`, termios raw-mode toggling, window-size ioctl/env fallback, and environment-aware color-depth helpers. `perf_hooks` exposes monotonic now/timeOrigin, marks/measures entries, histogram sample statistics with percentiles, and process CPU/wall-clock event-loop utilization estimates. `async_context_tracking` now exposes an explicit native async-context stack layered over `async_hooks` AsyncResource handles, with enter/exit helpers, execution/trigger async id lookup, depth tracking, snapshot JSON, and reset helpers, without claiming automatic callback propagation, Promise hooks, or `AsyncLocalStorage` semantics. `worker_threads` exposes main-thread metadata, environment data, MessageChannel/MessagePort queues, receiveMessageOnPort, and a main-thread postMessageToThread compatibility path. `child_process` executes real subprocesses for exec/execFile/spawn/spawnSync/fork-style helpers, including SA argv-vector parsing and real child pid reporting. `cluster` now exposes primary/worker metadata, scheduling-policy state, `setupPrimary()` metadata, JSON-based `setupPrimary()` config for `exec`/`cwd`/`env`, primary snapshot JSON, subprocess-backed worker fork handles, stdin/stdout message exchange, worker pid/alive/connected snapshots, signal-name kills, wait JSON, exited-after-disconnect state, and disconnect/kill/free helpers. `domain` now exposes native domain handles with explicit `enter()`/`exit()` stack state, member add/remove/count helpers, active-domain lookup, snapshot JSON, dispose, and free helpers without claiming JavaScript callback wrapping semantics. `deprecated` now exposes a native deprecation registry with idempotent code tracking, snapshot/clear/has helpers, and `NODE_OPTIONS`/environment-based deprecation flag introspection without claiming JavaScript wrapper or warning-event semantics. `command_line_options` now exposes host `argv` snapshots, whitespace-tokenized `NODE_OPTIONS` token snapshots, parsed `--env-file` / `--env-file-if-exists` / `--require` / `-r` / `--conditions` / `-C` / `--inspect*` metadata, and direct flag presence queries, strictly as native process-introspection helpers rather than Node CLI execution. `environment_variables` now exposes host environment snapshot JSON, single-variable existence/value queries, a native `.env` parser compatible with common Node `util.parseEnv()` cases, and `loadEnvFile`-style host environment loading that skips pre-existing variables and ignores `NODE_OPTIONS` entries from dotenv content. `errors` now exposes Node-style error code catalogs, system errno name/message lookup, system-error JSON with syscall/path metadata, and common `ERR_INVALID_ARG_TYPE` / `ERR_INVALID_ARG_VALUE` / `ERR_OUT_OF_RANGE` diagnostic JSON builders without claiming JavaScript `Error` subclass identity or stack semantics. `internationalization` now exposes native locale/timezone/environment snapshots, effective-locale resolution, encoding support metadata, `NODE_ICU_DATA` plus `--icu-data-dir` configuration introspection, and ICU-config detection without claiming bundled ICU collation/formatting engines or JavaScript `Intl` constructor support. `iterable_streams` now exposes stream-type catalogs, bridge capability metadata, explicit classic/web stream bridge operation lists, and stream-type/capability presence queries built on the existing native `stream` and `web_streams` helpers, without claiming JavaScript `Symbol.asyncIterator`, `Readable.from()`, or `fromWeb()/toWeb()` object-model semantics. `test_runner` now exposes built-in reporter metadata plus host `argv`/`NODE_OPTIONS` test flag introspection for coverage, watch, only, force-exit, isolation, concurrency, timeout, rerun-failures path, reporter names, and reporter destinations, backed by the native `sa test` workflow rather than a JavaScript `node:test` object model. `wasi` now exposes supported-version and import-module metadata plus host-config args/env/preopens/stdio/returnOnExit snapshots and `--allow-wasi` / experimental flag introspection, without claiming WebAssembly instantiation, `uvwasi` syscall bridging, or sandbox mount enforcement.
- Top-level `worker_threads` now exposes a native main-thread facade over the existing compatibility helpers: export-name metadata, `SHARE_ENV` metadata, `parentPort` null metadata, resource-limits JSON, environment-data helpers, `MessageChannel`/`MessagePort` queue handles, `receiveMessageOnPort`, and a main-thread `postMessageToThread` compatibility path. This remains honest main-thread/native-queue functionality rather than a claim of `Worker` construction, background JavaScript execution, `BroadcastChannel`, `LockManager`, structured-clone transfer registries, or EventTarget-style `MessagePort` objects.
- Top-level `perf_hooks` now exposes a native facade over the existing performance helpers: export-name metadata, supported entry-type metadata, static `constants` JSON, `performance` snapshot JSON, mark/measure entry snapshots, event-loop utilization JSON, histogram handles, and timerify metadata. This remains an honest native subset rather than a claim of JavaScript `Performance`/`PerformanceEntry`/`PerformanceObserver`/`PerformanceResourceTiming` class instances, observer callback buffering, or autonomous event-loop-delay sampling.
- Top-level `diagnostics_channel` now exposes a native facade over the existing channel helpers: export-name metadata, factory/support JSON for `channel()` and `tracingChannel()`, plus explicit feature metadata for subscribe/unsubscribe/publish/hasSubscribers/snapshot flows. This remains an honest native-handle subset rather than a claim of JavaScript `Channel`/`BoundedChannel` class instances, WeakRef-driven lifecycle, store binding, bounded channel windows, or `traceSync`/`tracePromise`/`traceCallback` wrappers.
- Top-level `tty` now exposes a native facade over the existing TTY helpers: export-name metadata, stdio TTY/color/window snapshots, and explicit feature metadata for `isatty`, `ReadStream`, `WriteStream`, `setRawMode`, `getWindowSize`, `getColorDepth`, and `hasColors`. This remains an honest native-handle subset rather than a claim of JavaScript `net.Socket` subclass instances, resize events, or `readline` cursor helper methods hanging off stream objects.
- Top-level `sea` now exposes a native facade over the existing SEA asset helpers: export-name metadata, provider/config snapshots, explicit feature metadata for `isSea`, `getAsset`, `getRawAsset`, `getAssetAsBlob`, and `getAssetKeys`, and honest limits around non-embedded providers. This remains an environment/directory-backed subset rather than a claim of parsing an actual embedded single-executable asset blob, a JavaScript `Blob` class instance, or full `TextDecoder` label/error semantics.
- Top-level `cluster` now exposes a native facade over the existing subprocess-backed cluster helpers: export-name metadata, `settings`/primary-config snapshot JSON, explicit scheduling constants and feature-support metadata for `setupPrimary`, `setupMaster`, and `fork`, plus honest limits for `Worker`, `worker`, `workers`, shared handle distribution, and internal IPC serialization. This remains primary-process configuration and explicit worker-handle functionality rather than a claim of JavaScript `EventEmitter` primary/worker objects, live worker registries, or libuv server sharing.
- Top-level `domain` now exposes an honest native facade over the existing explicit domain-stack helpers: export-name metadata, active-domain snapshot JSON, and explicit feature-support metadata for `Domain`, `create`, `createDomain`, `active`, and `_stack`. This remains explicit native-handle metadata rather than a claim of JavaScript `EventEmitter` inheritance, `process.domain` mutation hooks, `bind`/`intercept`/`run` callback wrapping, or async callback propagation.
- Top-level `report` now exposes a native facade over the existing diagnostic report helpers: export-name metadata, sample report snapshot JSON, config metadata for default filename and path resolution, and explicit feature-support metadata for `writeReport` and `getReport` plus honest unsupported property toggles. This remains explicit snapshot/file-write functionality rather than a claim of a live `process.report` getter/setter object, automatic signal/fatal-error/uncaught-exception hooks, or JavaScript `Error` object integration.
- Top-level `wasi` now exposes a native facade over the existing WASI config helpers: top-level export metadata, version/import-module/config snapshots, and explicit feature-support metadata that distinguishes supported host-config introspection from unsupported `WASI` class construction and WebAssembly execution flows. This remains configuration and permission metadata rather than a claim of `WASI` class instances, `wasiImport` objects, `start()`/`initialize()` execution, or live memory binding.
- Top-level `repl` now exposes a native facade over the existing session subset: export-name metadata, default prompt/terminal/color/builtin-command config, and explicit feature-support metadata for `start`, prompt/history/command registration, and explicit multiline buffering, alongside honest unsupported state for `REPLServer`, `Recoverable`, completion, context objects, and JavaScript evaluation. This remains explicit native session-handle functionality rather than a claim of stream-bound REPL server objects, `vm`-backed execution, or syntax-aware completion.
- Top-level `http` now exposes a native facade over the existing HTTP/1 helpers: export-name metadata, methods/status-code snapshots, max-header-size and parser-limit config metadata, and explicit feature-support metadata that distinguishes supported request/get/server/header-validation helpers from unsupported JavaScript `Agent`, `ClientRequest`, `IncomingMessage`, `OutgoingMessage`, `Server`, `ServerResponse`, and top-level `WebSocket` class semantics. This remains explicit native helper and metadata functionality rather than a claim of Node's asynchronous event-emitter object model or undici-backed top-level `WebSocket` export.
- Top-level `https` now exposes a native facade over the existing TLS-backed one-shot request helpers: export-name metadata, transport/config snapshots, and explicit feature-support metadata that distinguishes supported `request`/`get` JSON helpers from unsupported JavaScript `Agent`, `globalAgent`, `Server`, and `createServer` semantics. This remains explicit native helper metadata rather than a claim of Node's asynchronous HTTPS `ClientRequest`/Agent/server object model.
- Top-level `tls` now exposes a native facade over the existing TLS helpers: export-name metadata, default constants/cipher/root-certificate snapshots, config metadata for CA/secure-context/socket models, and explicit feature-support metadata that distinguishes supported cipher/CA/ALPN/secure-context/connect helpers from unsupported JavaScript `TLSSocket`, `SecureContext`, `Server`, `createServer`, and `checkServerIdentity` semantics. This remains explicit native helper and handle metadata rather than a claim of Node's full TLS class/event-emitter object model.
- Top-level `net` now exposes a native facade over the existing TCP and Unix socket helpers: export-name metadata, default auto-select-family snapshots, config metadata for explicit socket/server/BlockList/SocketAddress handles, and explicit feature-support metadata that distinguishes supported `connect`/`createConnection`/`createServer`, IP parsing, BlockList, SocketAddress, and auto-select-family settings from unsupported JavaScript `Server`, `Socket`, `Stream`, and internal `_createServerHandle`/`_normalizeArgs` helpers. This remains explicit native handle and metadata functionality rather than a claim of Node's EventEmitter socket/server object model or JavaScript class instances.
- Top-level `dns` now exposes a native facade over the existing resolver helpers: export-name metadata for `lookup`/`lookupService`/resolver methods/constants/`promises`, default server and result-order snapshots, config metadata for the explicit native `Resolver` handle model, and explicit feature-support metadata that distinguishes supported lookup/resolve/reverse/global-server/result-order flows from unsupported JavaScript callback scheduling, Promise object identity, and full c-ares channel behavior. This remains explicit native resolver functionality rather than a claim of JavaScript `Resolver` class instances, microtask timing, or libuv/c-ares event-loop semantics.
- Top-level `child_process` now exposes a native facade over the existing subprocess helpers: export-name metadata for `exec`/`execFile`/`execFileSync`/`execSync`/`fork`/`spawn`/`spawnSync`, config metadata for pid-only spawn and captured-stdio sync execution models, and explicit feature-support metadata that distinguishes supported real subprocess execution from unsupported JavaScript `ChildProcess` class instances, streaming stdio objects, IPC/message channels, `_forkChild`, and AbortSignal wiring. This remains explicit native subprocess functionality rather than a claim of Node's EventEmitter child-process object model or live stdio stream semantics.
- Top-level `async_hooks` now exposes a native facade over the existing explicit async-context helpers: export-name metadata for `executionAsyncId`, `triggerAsyncId`, `AsyncResource`, `asyncWrapProviders`, `createHook`, `executionAsyncResource`, and `AsyncLocalStorage`, config metadata for explicit native `AsyncResource` handles and snapshot JSON, and explicit feature-support metadata that distinguishes supported id lookup and resource snapshots from unsupported JavaScript hook callback registration, execution resource object identity, and `AsyncLocalStorage` store propagation. This remains explicit native async-context metadata rather than a claim of Node's callback instrumentation or JavaScript async resource object model.
- Top-level `stream` now exposes a native facade over the existing explicit stream-handle helpers: export-name metadata for `Readable`/`Writable`/`Duplex`/`Transform`/`PassThrough` plus `pipeline`, `finished`, `compose`, `destroy`, and `duplexPair`, config metadata for explicit native handle and summary-JSON models, and explicit feature-support metadata that distinguishes supported native duplex/transform/passthrough/pipeline flows from unsupported JavaScript stream class instances, EventEmitter lifecycle, `stream.promises`, `addAbortSignal`, and default high-water-mark tuning. This remains explicit native stream-handle functionality rather than a claim of Node's object/prototype/event stream model.
- Top-level `readline` now exposes a native facade over the existing terminal-control and `readline.promises` helpers: export-name metadata for cursor/clear helpers, `emitKeypressEvents`, and `promises`, config metadata for fd-based terminal operations plus the explicit native promises `Interface` handle model, and explicit feature-support metadata that distinguishes supported terminal control and native question/close/snapshot flows from unsupported JavaScript `Interface` class instances, callback/event lifecycles, completers, and interactive history editing. This remains explicit native terminal/prompt helper functionality rather than a claim of Node's full readline editor object model.
- Top-level `timers` now exposes a native facade over the existing timer-registry and `timers.promises` helpers: export-name metadata for `setTimeout`/`clearTimeout`/`setImmediate`/`clearImmediate`/`setInterval`/`clearInterval` plus `promises`, config metadata for numeric timer ids and explicit promises interval handles, and explicit feature-support metadata that distinguishes supported timer-id bookkeeping and native wait/yield/interval-handle flows from unsupported JavaScript `Timeout`/`Immediate` objects, callback scheduling fidelity, AbortSignal cancellation, and Promise object identity. This remains explicit native timer-helper functionality rather than a claim of Node's event-loop timer object model.
- Top-level `console` now exposes a native facade over the existing console helpers: export-name metadata for common global console functions, config metadata for stdout/stderr writes, count registry state, and timing/group helper models, and explicit feature-support metadata that distinguishes supported `log`/`info`/`debug`/`warn`/`error`/`dir`/`dirxml`/`table`/`trace`/`assert`/`count`/`countReset`/`group*`/`timeLog`/`timeStamp` flows from unsupported JavaScript `Console` construction plus `time`/`timeEnd` registry semantics. This remains explicit native output-helper functionality rather than a claim of Node's stream-backed `Console` class, inspect/colorMode options, or exact formatting semantics.
- Top-level `events` now exposes a native facade over the existing explicit emitter helpers: export-name metadata for `EventEmitter`, listener inspection helpers, and Node-style static properties, config metadata for explicit native emitter handles with stored listener arrays and max-listener metadata, and explicit feature-support metadata that distinguishes supported native emitter handle operations from unsupported JavaScript `EventEmitter` class instances, symbol semantics, `events.once()` Promise behavior, `events.on()` AsyncIterator behavior, `addAbortListener`, `captureRejections`, and `EventEmitterAsyncResource`. This remains explicit native emitter-handle functionality rather than a claim of Node's JavaScript object model or async iterator semantics.
- Top-level `trace_events` now exposes a native facade over the existing tracing handle helpers: export-name metadata for `createTracing`, `getEnabledCategories`, and `Tracing`, config metadata for explicit native tracing handles with category text and enabled state, and explicit feature-support metadata that distinguishes supported handle allocation and enable/disable/category snapshot flows from unsupported JavaScript `Tracing` class construction, custom inspect behavior, leak warnings, and process-wide trace backend integration. This remains explicit native tracing-handle functionality rather than a claim of Node's internal trace_events backend.
- Top-level `fs` now exposes a native facade over the existing filesystem helpers: export-name metadata, fs constants snapshot JSON, config metadata for explicit sync-style path/fd helpers plus already-resolved `fs.promises` compatibility entry points, and explicit feature-support metadata that distinguishes supported stat/readdir/readFile/writeFile/mkdir/rm/unlink/rename/copyFile/realpath/readlink/opendir/open/statfs/glob/promises/constants flows from unsupported JavaScript `ReadStream`/`WriteStream`/`Dir`/`Dirent`/`Stats` class instances, `watch`/`watchFile`/`unwatchFile`, `cp`, and `openAsBlob`. This remains explicit native filesystem-helper functionality rather than a claim of Node's callback scheduling, watcher eventing, Blob construction, or stream object model.
- Top-level `crypto` now exposes a native facade over the existing crypto helpers: export-name metadata, hash-catalog snapshot JSON, config metadata for random/hash/HMAC/cipher/key/WebCrypto handle models, and explicit feature-support metadata that distinguishes supported random/hash/HMAC/PBKDF2/HKDF/scrypt/cipher/decipher/sign/verify/generateKey/getHashes/timingSafeEqual/webcrypto/subtle flows from unsupported JavaScript `Hash`/`Hmac`/`Cipheriv`/`Decipheriv`/`KeyObject`/`Certificate`/`X509Certificate` class semantics, `generateKeyPair`, PEM/DER/JWK key-object construction, and secure-heap accounting. This remains explicit native helper and handle functionality rather than a claim of Node's callback timing, Promise identity, or browser-style `crypto` object graphs.
- Top-level `util` now exposes a native facade over the existing utility helpers: export-name metadata, config metadata for format/inspect/wrapper/parseArgs/MIME/deprecation models, and explicit feature-support metadata that distinguishes supported `format`, `inspect`, `debuglog`, `deprecate`, `callbackify`, `promisify`, `inherits`, `isDeepStrictEqual`, `stripVTControlCharacters`, `parseArgs`, `diff`, `styleText`, and path-based `MIMEType` lookup from unsupported JavaScript `TextEncoder`/`TextDecoder`/`types` object semantics, WHATWG `MIMEParams` classes, `formatWithOptions`, `getCallSites`, system-error map/name/message getters, `parseEnv`, `setTraceSigInt`, and abort-transfer helpers. This remains explicit native helper metadata rather than a claim of JavaScript function identity, Promise timing, stack inspection, or WHATWG object-model parity.
- Top-level `buffer` now exposes a native facade over the existing buffer helpers: export-name metadata, config metadata for text/classification/object-url/static-helper models, and explicit feature-support metadata that distinguishes supported `transcode`, `isUtf8`, `isAscii`, `atob`, `btoa`, `resolveObjectURL`, plus core `byteLength` and `concat` helper coverage, from unsupported JavaScript `Buffer`/`Blob`/`File` class semantics, `constants`, `kMaxLength`, `kStringMaxLength`, and mutable `INSPECT_MAX_BYTES` state. This remains explicit byte-slice helper metadata rather than a claim of Node's `Uint8Array` subclass object model, blob URL registry behavior, or live constructor exports.
- Top-level `url` now exposes a native facade over the existing URL helpers: export-name metadata, config metadata for legacy parse/format/resolve plus explicit URL handle models, and explicit feature-support metadata that distinguishes supported `parse`, `format`, `resolve`, and explicit `URL` handle snapshots from unsupported WHATWG `URLSearchParams`, `URLPattern`, `domainToASCII`, `domainToUnicode`, `pathToFileURL`, `fileURLToPath`, `fileURLToPathBuffer`, `urlToHttpOptions`, and `canParse` exports. This remains explicit string/JSON/helper metadata rather than a claim of Node's WHATWG URL object graph, live property mutation semantics, or full file-URL conversion surface.
- Top-level `process` now exposes a native facade over the existing process helpers: export-name metadata, config metadata for identity/memory/signal/env models, and explicit feature-support metadata that distinguishes supported `pid`, `ppid`, `cwd`, `uptime`, `hrtime`, `memoryUsage`, `argv`, `versions`, `env`, `getuid`, `getgid`, `kill`, `resourceUsage`, `availableMemory`, `constrainedMemory`, `features`, and `exit` from unsupported `arch`, `platform`, `release`, `version`, `stdin`, `stdout`, `stderr`, `nextTick`, `emitWarning`, `dlopen`, `umask`, and `chdir` semantics. This remains explicit native process snapshot and control metadata rather than a claim of Node's EventEmitter process object, stdio stream objects, warning events, or nextTick runtime queues.
- Top-level `os` now exposes a native facade over the existing OS helpers: export-name metadata, config metadata for snapshot/identity/resource/constants models, and explicit feature-support metadata that distinguishes supported `arch`, `availableParallelism`, `cpus`, `endianness`, `freemem`, `getPriority`, `homedir`, `hostname`, `loadavg`, `machine`, `networkInterfaces`, `platform`, `release`, `setPriority`, `tmpdir`, `totalmem`, `type`, `uptime`, `userInfo`, `version`, and `constants` from unsupported `devNull` and `EOL` constants. This remains explicit native host snapshot metadata rather than a claim of frozen JavaScript constants objects, lazy getter primitives, or Node's exact cross-build target identity semantics.
- Top-level `path` now exposes a native facade over the existing path helpers: export-name metadata, config metadata for join/analysis/separator/glob models, and explicit feature-support metadata that distinguishes supported `join`, `resolve`, `normalize`, `basename`, `dirname`, `extname`, `isAbsolute`, `relative`, `format`, `parse`, `sep`, `delimiter`, and `matchesGlob` from unsupported `posix`, `win32`, and `toNamespacedPath` namespace semantics. This remains explicit native path-helper metadata rather than a claim of Node's distinct `path.posix`/`path.win32` method tables, Windows namespaced path behavior, or full cross-platform normalization edge-case parity.
- Top-level `querystring` now exposes a native facade over the existing query-string helpers: export-name metadata, config metadata for percent-escape/unescape, flat-object parse/stringify, and byte-returning `unescapeBuffer` models, and explicit feature-support metadata that distinguishes supported `escape`, `unescape`, `parse`, `stringify`, `encode`, `decode`, and `unescapeBuffer` flows from unsupported repeated-key array coercion, custom separator/option bags, JavaScript `Buffer` instance identity, and full prototype-guard parity. This remains explicit native string/JSON/byte helper functionality rather than a claim of Node's mutable JavaScript object model or complete legacy `querystring` edge-case semantics.
- Top-level `punycode` now exposes a native facade over the existing punycode and IDNA helpers: export-name metadata, config metadata for `encode`/`decode` plus `toASCII`/`toUnicode`, and explicit feature-support metadata that distinguishes supported UTF-8 punycode conversion and libidn2-backed domain conversion from unsupported `ucs2` namespace helpers, deprecated `version` constant export, and JavaScript deprecation-warning or exception-object semantics. This remains explicit native string/domain helper functionality rather than a claim of Node's deprecated JavaScript `punycode` object graph or UTF-16 code-unit array helpers.
- Top-level `string_decoder` now exposes a native facade over the existing decoder handle helpers: export-name metadata, config metadata for explicit `StringDecoder` handle allocation plus `write`, and explicit feature-support metadata that distinguishes supported multibyte UTF-8 chunk decoding from unsupported `end`, `text`, `lastChar`, `lastNeed`, `lastTotal`, constructor encoding selection, and JavaScript instance/prototype semantics. This remains explicit native decoder-handle functionality rather than a claim of Node's JavaScript `StringDecoder` object model or full codec-selection behavior.
- Top-level `zlib` now exposes a native facade over the existing compression helpers: export-name metadata, config metadata for synchronous gzip/gunzip/deflate/inflate/raw/unzip/Brotli/Zstd plus CRC32, and explicit feature-support metadata that distinguishes supported synchronous buffer transforms from unsupported `create*` stream constructors, callback-style async convenience methods, and `constants`/`codes` object exports. This remains explicit native buffer-transform functionality rather than a claim of Node's `Transform` stream object model, flush-state machines, or full top-level constants tables.
- Top-level `dgram` now exposes a native facade over the existing UDP helpers: export-name metadata, socket/config snapshots, and explicit feature-support metadata that distinguishes supported socket creation, bind/send/recv/connect/disconnect/address/ref/multicast/blocklist helpers from unsupported JavaScript `Socket` EventEmitter class semantics. This remains explicit native UDP handle functionality rather than a claim of Node's callback/event-driven `dgram.Socket` object model.
- Top-level `http2` now exposes a native facade over the existing HTTP/2 helpers: export-name metadata, default-settings and sensitive-header snapshots, `nghttp2` availability/config metadata, and explicit feature-support metadata that distinguishes the supported settings/constants subset and cleartext h2c request helper from unsupported server/session/request/response object semantics. This remains explicit native metadata and helper functionality rather than a claim of JavaScript `ClientHttp2Session`/`Http2Server` objects, TLS ALPN negotiation, stream lifecycle events, or push-stream runtime behavior.
- Top-level `test` now exposes a native compatibility facade over the existing `test_runner` and `assert` subsets. It reports Node-style top-level export names (`test`, `it`, `suite`, `describe`, lifecycle hook names, `run`, `getTestContext`, `assert`, `mock`, `snapshot`), alias metadata, built-in reporters, and resolved test-runner config, plus explicit support JSON for `assert`, `mock`, `snapshot`, and `getTestContext`/`run` properties. This remains honest metadata for the native `sa test` harness rather than a claim of JavaScript callback scheduling, `TestContext` objects, mock interception, or snapshot serializer hooks.
- Top-level `module` now exposes a native facade for the parts of `node:module` that can be implemented honestly without a JS loader runtime: export-name metadata, builtin module name listing, `isBuiltin`, `constants.compileCacheStatus`, global path snapshots, nearest `package.json` lookup, compile-cache directory/status metadata, and source-map support configuration snapshots/setters. This remains a configuration/filesystem subset rather than a claim of `createRequire`, ESM loader registration, `runMain`, `SourceMap` object construction, or TypeScript syntax stripping semantics.
- Top-level `assert` now exposes a native facade over the existing assertion helpers: export-name metadata, config metadata for explicit status-bit plus AssertionError-JSON results, and explicit feature-support metadata that distinguishes supported `ok`, `equal`, `strictEqual`, `deepStrictEqual`, `fail`, and `strict` snapshot flows from unsupported `throws`, `rejects`, `Assert` class construction, and JavaScript exception identity semantics. This remains explicit native assertion-result functionality rather than a claim of Node's throwing `assert` function table or Promise assertion behavior.
- Top-level `constants` now exposes a native facade over the existing aggregated constants helper: export-name metadata derived from the current native constants snapshot, config metadata for `os.constants` plus fs and crypto additions, and explicit feature-support metadata that distinguishes supported OS/fs/hash constant aggregation from unsupported frozen object and JavaScript property-descriptor semantics. This remains explicit native constants-snapshot functionality rather than a claim of Node's frozen `constants` object identity or lazy property behavior.
- Top-level `inspector` now exposes an honest native configuration facade over existing command-line and permission parsing: export-name metadata, `--inspect*` flag snapshots, resolved host/port/wait mode metadata, enabled/allowed booleans, and feature-support JSON. This remains configuration introspection only rather than a claim of live inspector activation, CDP/WebSocket sessions, `Session`, inspector `console`, `Network`/`NetworkResources`, `DOMStorage`, or debugger blocking semantics.
- Top-level `assert`, `constants`, and `sys` modules now expose honest native subsets instead of remaining entirely absent. `assert` provides explicit `ok`, `equal`/`strictEqual`, `deepStrictEqual`, `fail`, status, and strict-config helpers that return success bits plus `AssertionError` JSON payloads and diff metadata rather than throwing JavaScript exceptions. `constants` exports a flattened aggregate JSON assembled from `os.constants`, standard fs access/copyfile flags, and the native crypto hash catalog, following Node's module shape where practical without claiming a frozen JS object. `sys` exposes deprecated-`util` alias metadata plus direct wrappers for native `util.format`, `util.inspect`, and `util.debuglog`, including `DEP0025` deprecation JSON rather than runtime warning events.
- `zlib` exposes synchronous gzip/gunzip, deflate/inflate, raw deflate/inflate, unzip, Brotli compress/decompress, Zstd compress/decompress, and CRC32 helpers using `std.compress` plus dynamically loaded Brotli/Zstd system libraries.
- `web_streams` exposes synchronous byte-buffer helpers for readable, writable, and transform stream handles, including enqueue/read/write/snapshot/close/free operations.
- `sqlite` exposes a native synchronous subset through dynamically loaded `libsqlite3`: version, open/close, exec, query JSON, prepare, typed bind int/double/text/blob/null, step, reset, clearBindings, finalize, changes, lastInsertRowid, online backup init/step/remaining/finish plus backup-to-file helpers, session changeset/patchset/isEmpty/memoryUsed/close/applyChangeset helpers, and an SQLTagStore-style prepared statement cache with all/get/run/snapshot/clear/free.
- `ffi` exposes a native dynamic-library helper subset through `std.DynLib`: open/close, symbol existence, common C ABI integer calls, C string length calls, string/integer calls, and string pointer return helpers.
- `vfs` exposes an in-memory `MemoryProvider`-style virtual filesystem subset with mount-like handles, cwd/chdir, mkdir, write/append/read file, file-handle open/read/write/fstat/ftruncate/close, dir-handle opendir/read/close/snapshot, watch/watcher next/snapshot/close event queues, readdir, stat/lstat JSON, exists, unlink, recursive rm, rename, copyFile, symlink/readlink, realpath, provider snapshot, and free helpers.

## Explicitly Excluded

- `vm`, `v8`, `module` loader execution, live `inspector` protocol/session behavior, `debugger`, and Node internal bootstrap/realm behavior are out of scope because they require a JavaScript runtime or V8 semantics. `repl` now exposes a native session subset for prompt/history/dot-command handling, but not JavaScript evaluation.

## Remaining Strict Node Gaps

- Full `http2` session/server/TLS ALPN/stream/push runtime semantics, QUIC/HTTP3 handshake/session/stream/request transport, `tls` socket/server object model, and DTLS handshake/record-layer crypto still require mature native protocol library integration.
- Full c-ares channel behavior such as search domains/rotation/cancelation, true JavaScript Promise object identity for `dns.promises`, and full Node DNS error-code semantics still need c-ares-level compatibility.
- `events`, `streams`, `timers`, `readline`, `worker_threads`, `cluster`, `domain`, and `test` need a shared event loop and native callback dispatch model for strict Node behavior; strict timer ref/unref, AbortSignal cancellation, Promise object identity, TTY raw-mode behavior, WHATWG Web Streams locking, backpressure, reader/writer, pipe, tee, and async iteration semantics are not complete. `cluster` currently models real subprocess workers and simple stdin/stdout messaging only; it does not yet expose Node's EventEmitter worker/server objects, shared-listener distribution, or internal IPC serialization. `domain` currently models explicit native domain stack handles and membership metadata only; it does not yet expose EventEmitter inheritance, bind/intercept wrappers, or async callback propagation across other module callbacks. `repl` currently models explicit native sessions, prompts, line history, dot-command registration, and buffered continuation input only; it does not expose JavaScript context objects, syntax recovery, completion, or `vm`-backed evaluation.
- Full WASI runtime behavior such as WebAssembly instantiation/start/initialize/finalize flows, `uvwasi` syscall bridging, memory binding, and enforced preopen sandbox semantics, real OS-backed VFS mounts and complete VFS stream/watch async/event-emitter APIs, full libffi type/signature varargs/callback support, SQLite session filter/conflict callback semantics and strict JS SQLTagStore template-literal semantics, and full Web Crypto RSA/ECDSA/ECDH/KDF/wrapKey/unwrapKey/JWK/structured `CryptoKey` semantics need first-class ABI surfaces and tests.
- Node-compatible error classes/codes and option validation are not yet complete across modules.

## Verification Commands

- `zig build`
- `zig build test`
- `comm -23 <(rg '^@extern ' node.sai node_extra.sai | sed -E 's/.*@extern ([A-Za-z0-9_]+).*/\1/' | sort -u) <(nm -D --defined-only zig-out/lib/libnode.so | awk '{print $3}' | sort -u)`
