// SA tests for SEA extras: status_json, get_raw_asset, get_asset_as_blob
@import "../node.sal"
@import "../node_extra.sal"
@const S_KEY = utf8:"test-key"

@test "node.sea status_json"():
L_ENTRY:
    op = add 0, 0
    ol = add 0, 0
    s = add 0, 0
    EXPAND NODE_SEA_STATUS_JSON op, ol, s
    !s
    !ol
    !op
    return
