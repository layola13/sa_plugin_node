// SA tests for Node worker_threads: environment_data, resource_limits_json
@import "../node.sal"
@const S_KEY = utf8:"test"

@test "node.worker_threads resource_limits_json"():
L_ENTRY:
    op = add 0, 0
    ol = add 0, 0
    s = add 0, 0
    EXPAND NODE_WORKER_THREADS_RESOURCE_LIMITS_JSON op, ol, s
    !s
    !ol
    !op
    return
